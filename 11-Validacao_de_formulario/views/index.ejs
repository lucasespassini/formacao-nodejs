<!DOCTYPE html>
<html lang="pt-br">
<head>
   <meta charset="UTF-8">
   <meta http-equiv="X-UA-Compatible" content="IE=edge">
   <meta name="viewport" content="width=device-width, initial-scale=1.0">
   <title>Document</title>
   <style>
      * {
         margin: 0;
         padding: 0;
      }

      body {
         font-family: sans-serif;
      }

      form {
         padding: 20px;
         position: absolute;
         top: 50%;
         left: 50%;
         transform: translate(-50%, -50%);
         border-radius: 10px;
         background-color: #ccc;
      }

      h1 {
         text-align: center;
         margin-bottom: 15px
      }

      input {
         margin: 5px 0;
         padding: 5px;
         outline: none;
         border: none;
         border-radius: 5px;
      }

      button {
         padding: 7px;
         display: block;
         margin: 0 auto;
         margin-top: 15px;
         border: none;
         border-radius: 5px;
         color: #fff;
         background-color: green;
         cursor: pointer;
         transition: .2s;
      }

      button:hover {
         color: #000;
         background-color: greenyellow;
      }
   </style>
</head>
<body>
   <form method="post" action="/form">
      <h1>Formulário</h1>
      <input type="email" name="email" id="email" placeholder="Email" value="<%= email %>"><br>
      <span><%= emailError %></span>
      <input type="text" name="name" id="name" placeholder="Nome"><br>
      <span><%= nameError %></span>
      <input type="number" name="pontos" id="pontos" placeholder="Pontos"><br>
      <span><%= pontosError %></span>
      <button type="submit">Enviar</button>
   </form>
</body>
</html>